# AuthModules organization profile

This repository stores public GitHub organization profile content and shared community defaults for AuthModules.

The organization profile shown at `github.com/authmodules` is maintained in:

```text
profile/README.md
```

Project roadmap, architecture, package map, migration notes, and cross-repository planning belong in the umbrella repository:

```text
authmodules/authmodules
```
